<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/images/flash-card-icon.png">

    <!-- Load App Styles -->
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <header>
      <div class="header-content">
        <div class="title">
          <img src="/images/flash-card-icon.png" alt="Title Icon">
          <h1><%= title %></h1>
        </div>
        <div class="edit-stack">
          <% if (pageID == "home") { %>
            <a href="/edit" class="action-btn">
              <i class="fas fa-pencil-alt"></i> Edit Card Stack
            </a>
          <% } else if (pageID == "edit") { %>
            <a href="/" class="action-btn">
              <i class="fa-solid fa-arrow-left"></i> Back
            </a>
          <% } %>          
        </div>
      </div>
    </header>
    
    

    <div class="container">        